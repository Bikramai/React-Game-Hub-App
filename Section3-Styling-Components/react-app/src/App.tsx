import Button from "./components/Button/Button";

function App() {
  return (
    <div>
      <Button color="primary" onClick={() => true}>
        My Button
      </Button>
    </div>
  );
}

export default App;
